# 概要设计数据结构设计生成命令 (Chapter 5)

## 命令描述
生成模块概要设计说明书的第5章"数据结构设计",包括配置文件定义和全局数据结构定义。

**核心特性**:
-  **多语言支持**: 支持Python、Java、Go、PHP等主流编程语言的数据结构检索
- **增量开发优先**: 优先检索和复用现有代码中的数据结构实现
- **统一规范输出**: 无论何种语言,输出统一格式的设计文档
- **向后兼容保障**: 确保数据结构设计的一致性和向后兼容性

## 命令语法
```bash
/chapter5 $1
```

## 参数说明

- **$1**: 必填参数,需求分析文档路径,指向包含数据结构需求的需求文档

## 输出内容

**完成设计文档章节5**:

- `5. 数据结构设计`
  - `5.1 配置文件定义`
  - `5.2 全局数据结构定义`

## 输出文件

追加到现有设计文档:`doc/design/tech_design.md`

## ⚠️ 核心强制约束（必须严格遵守）

### 1. Prompt文件步骤严格执行原则

**❌ 禁止的行为**：

- 跳过Prompt文件中定义的任何步骤或子步骤
- 合并多个步骤一起执行
- 只生成内容而不写入文件
- 不生成Prompt要求的检查点文件（`xxx_check.md`）
- 不执行Prompt要求的验证操作

**✅ 必须的行为**：

- **设计模板**: templates/design_template.md
- **设计checklist**: references/specifications/设计方案checklist.md
- **代码风格**: 参考当前路径项目代码中现有代码的数据结构风格
- **配置规范**: 参考当前路径项目代码中现有配置文件格式

- **逐步执行**：严格按照Prompt文件中定义的步骤1、步骤2、步骤3...顺序执行
- **子步骤不跳过**：每个步骤内的1.1、1.2、1.3等子步骤必须全部执行
- **检查点必生成**：Prompt中要求生成的`xxx_check.md`检查点文件必须生成
- **写入必验证**：每次使用Edit/Write工具写入后，必须使用Read工具验证写入成功
- **失败必重试**：验证失败时必须重新执行写入操作，直到成功

### 增量开发优先原则
```
数据结构设计采用"知识库优先、项目代码辅助"的检索策略,处理流程:
1. 在需求文档中检索需求的所属业务仓库
2. 优先在 {所属业务仓库}/doc/kb/技术知识库/数据结构.md 中检索需求文档$1中列举的数据结构
3. 配合在当前路径项目代码中检索进行补充和验证
4. 能检索到的数据结构,按照需求描述对已存在的结构进行扩展,并输出设计
5. 检索不到的数据结构,按照需求描述进行全新设计,参考现有代码风格
6. 所有输出表格必须包含"来源"列,标注数据结构来源(知识库/项目代码)
```

### 章节完整性要求
- 所有配置项必须明确定义类型、默认值、说明
- 全局数据结构必须描述用途、字段定义、约束条件
- 如果不涉及配置文件或全局数据结构,需明确写明"不涉及"及原因

## 执行流程

**⚠️ 核心执行原则（必须严格遵守）**：

1. **强制写入原则**：每个步骤完成后，**必须**使用Edit/Write工具将生成的内容写入目标文件，不能只生成内容而不保存
2. **验证确认原则**：每次写入后，**必须**使用Read工具读取目标文件验证内容已正确写入
3. **失败重试原则**：如果验证失败，**必须**重新执行写入操作，直到验证通过
4. **完整性原则**：你的上下文窗口会在接近限制时自动被压缩，因此不要因为Token预算问题提前停止任务，即使预算快用完，也要尽可能完整执行任务
5. **检查点文件原则**：生成的检查点文件`xxx_check.md`保留，不允许删除
6. **检查点原则**：每个子步骤都有"完成检查点"，**必须**在该检查点执行强制写入和验证操作

### 步骤1: 数据结构需求提取与分类

**执行目标**: 从需求文档中提取所有数据结构相关需求,并分类识别。

**执行内容**:

#### 1. 读取需求文档
```
输入: $1 需求分析文档路径
处理: 提取所有数据结构相关需求
```

#### 2. 数据结构需求分类
从需求文档中识别和分类数据结构需求:

**数据结构需求分类**:

1. **配置文件需求**
   - 新增配置项
   - 修改现有配置项
   - 配置文件格式变更
2. **全局数据结构需求**
   - 新增全局变量/常量
   - 新增通信消息结构
   - 修改现有数据结构
3. **表结构需求**
   - 新增数据库表
   - 修改现有表结构
   - 新增表字段
   - 修改表字段定义

#### 3. 完成检查点（强制执行）

**⚠️ 本步骤为分析步骤，不需要写入目标文件**

**验证清单**：
- [ ] 已读取需求文档$1
- [ ] 已识别所有配置文件需求
- [ ] 已识别所有全局数据结构需求
- [ ] 已识别所有表结构需求
- [ ] 已对需求进行分类整理

**如果分析不完整**：
- 重新读取需求文档
- 补充遗漏的数据结构需求
- 确保分类清晰准确

------

### 步骤2: 现有数据结构检索与分析

**执行目标**: 优先在知识库中检索数据结构定义,再配合项目代码检索,为增量开发提供基础。

**执行原则**: 你的上下文窗口会在接近限制时自动被压缩,因此不要因为Token预算问题提前停止任务,即使预算快用完,也要尽可能完整执行任务。

**检索策略**:

1. **主要检索**: 优先使用 `{所属业务仓库}/doc/kb/技术知识库/数据结构.md` 进行快速检索
2. **辅助检索**: 配合当前路径项目代码目录进行检索验证和补充

```
知识库文档清单 = [
  "{所属业务仓库}/doc/kb/技术知识库/*",
  "{所属业务仓库}/doc/kb/业务知识库/*"
]
```

**执行内容**:

#### 1. 优先检索知识库数据结构文档

**检索目标**: 从知识库中快速获取已归档的数据结构定义。

**检索步骤**:

```python
# 1. 读取知识库数据结构文档
Read(file_path="{所属业务仓库}/doc/kb/技术知识库/数据结构.md")

# 2. 从知识库中提取需求相关的数据结构信息:
# - 表结构定义
# - 字段定义和类型
# - 索引和约束
# - 配置文件定义
```

**知识库检索优势**:
- ✅ **快速定位**: 知识库已整理归档,检索速度快
- ✅ **信息集中**: 数据结构定义集中在一个文档中
- ✅ **结构清晰**: 按模块分类整理,易于理解
- ✅ **版本明确**: 包含当前生产环境的数据结构版本

**输出表格**:
| 数据结构名称 | 类型 | 用途 | 来源 | 代码位置 |
|------------|------|------|------|---------|
| xxx_table | 数据库表 | xxx存储 | 知识库 | xxx/aaa.py:20 |
| xxxModel | ORM模型 | xxx模型 | 知识库 | xxx/aaa.py:25 |

**注意**: 如果知识库文档不存在或内容不完整,跳过此步骤,直接进行项目代码检索。

------

#### 2. 配置文件检索(辅助检索)

**检索目标**: 在项目代码中检索配置文件定义,作为知识库检索的补充和验证。

**检索内容**:
- 配置文件路径和格式
- 现有配置项定义
- 配置加载和验证机制
- 知识库中未覆盖的配置项

**多语言支持**:
- **Python**: .py, .yaml, .toml, .ini, .json
- **Java**: .properties, .xml, .yaml, .json
- **Go**: .yaml, .toml, .json, .go
- **PHP**: .php, .ini, .yaml, .json

**检索方法**:

**步骤1: 搜索配置文件目录和文件**
```python
# 搜索所有可能的配置文件
Glob(pattern="**/config*/**/*")
Glob(pattern="**/conf*/**/*")
Glob(pattern="**/settings*/**/*")
Glob(pattern="**/*.yaml")
Glob(pattern="**/*.yml")
Glob(pattern="**/*.json")
Glob(pattern="**/*.toml")
Glob(pattern="**/*.properties")
Glob(pattern="**/*.ini")
```

**步骤2: 搜索配置类/结构定义 (根据项目语言选择对应模式)**

**Python项目**:
```python
Grep(
  pattern="class.*Config.*\\(BaseSettings|BaseModel\\):",
  glob="**/*.py",
  output_mode="content",
  -A=30
)
```

**Java项目**:
```python
Grep(
  pattern="@Configuration|@ConfigurationProperties",
  glob="**/*.java",
  output_mode="content",
  -A=30
)
```

**Go项目**:
```python
Grep(
  pattern="type.*Config.*struct",
  glob="**/*.go",
  output_mode="content",
  -A=30
)
```

**PHP项目**:
```python
Grep(
  pattern="class.*Config",
  glob="**/*.php",
  output_mode="content",
  -A=30
)
```

**检索信息**:
- **配置文件路径**: 配置文件的存储位置
- **配置格式**: Python类、JSON、YAML、TOML等
- **配置项定义**: 字段名、类型、默认值、验证规则
- **配置分组**: 数据库配置、服务配置、业务配置等

**输出表格**:
| 配置类名 | 配置项 | 类型 | 默认值 | 说明 | 来源 | 代码位置 |
|---------|-------|------|--------|------|------|---------|
| xxxConfig | xxx_host | str | "localhost" | xxx主机地址 | 项目代码 | aaa/xxx.py:15 |
| xxxConfig | xxx_port | int | 5432 | xxx端口 | 项目代码 | aaa/xxx.py:16 |

#### 3. 全局数据结构检索(辅助检索)

**检索目标**: 在项目代码中检索全局数据结构定义,作为知识库检索的补充和验证。

**检索内容**:

- ORM模型/实体类定义
- 数据模型/DTO定义
- 全局常量和枚举
- 消息格式定义
- 知识库中未覆盖的数据结构

**多语言支持**:
- **Python**: ORM(SQLAlchemy/Django), Pydantic, Dataclass, Enum
- **Java**: Entity(JPA/Hibernate), DTO, Enum
- **Go**: Struct, Enum(iota)
- **PHP**: Entity(Doctrine), Model, Enum

**检索方法** (根据项目语言选择对应检索模式):

**Python项目**:
```python
# 1. 搜索ORM模型 (SQLAlchemy)
Grep(
  pattern="class.*\\(Base\\):|@declarative_base",
  glob="**/model*/**/*.py",
  output_mode="content",
  -A=40
)

# 2. 搜索Django模型
Grep(
  pattern="class.*\\(models\\.Model\\):",
  glob="**/model*/**/*.py",
  output_mode="content",
  -A=40
)

# 3. 搜索Pydantic/DTO模型
Grep(
  pattern="class.*\\(BaseModel\\):",
  glob="**/*.py",
  output_mode="content",
  -A=25
)

# 4. 搜索枚举定义
Grep(
  pattern="class.*\\(Enum\\):|class.*\\(IntEnum\\):",
  glob="**/*.py",
  output_mode="content",
  -A=15
)

# 5. 搜索消息结构
Grep(
  pattern="class.*Message.*\\(BaseModel\\):|@dataclass",
  glob="**/*schema*/**/*.py",
  output_mode="content",
  -A=30
)
```

**Java项目**:
```python
# 1. 搜索JPA实体类
Grep(
  pattern="@Entity|@Table",
  glob="**/entity/**/*.java",
  output_mode="content",
  -A=40
)

# 2. 搜索DTO类
Grep(
  pattern="class.*DTO|class.*Dto",
  glob="**/dto/**/*.java",
  output_mode="content",
  -A=25
)

# 3. 搜索枚举定义
Grep(
  pattern="enum ",
  glob="**/*.java",
  output_mode="content",
  -A=15
)

# 4. 搜索消息/事件类
Grep(
  pattern="class.*Event|class.*Message",
  glob="**/event/**/*.java",
  output_mode="content",
  -A=30
)
```

**Go项目**:
```python
# 1. 搜索模型结构体
Grep(
  pattern="type.*struct\\s*\\{",
  glob="**/model*/**/*.go",
  output_mode="content",
  -A=40
)

# 2. 搜索DTO结构体
Grep(
  pattern="type.*DTO.*struct|type.*Dto.*struct",
  glob="**/*.go",
  output_mode="content",
  -A=25
)

# 3. 搜索枚举定义(iota)
Grep(
  pattern="const.*iota",
  glob="**/*.go",
  output_mode="content",
  -A=15
)

# 4. 搜索消息结构体
Grep(
  pattern="type.*Message.*struct|type.*Event.*struct",
  glob="**/*.go",
  output_mode="content",
  -A=30
)
```

**PHP项目**:
```python
# 1. 搜索Doctrine实体类
Grep(
  pattern="@Entity|@Table|@ORM",
  glob="**/Entity/**/*.php",
  output_mode="content",
  -A=40
)

# 2. 搜索模型类
Grep(
  pattern="class.*Model|class.*DTO",
  glob="**/Model*/**/*.php",
  output_mode="content",
  -A=25
)

# 3. 搜索枚举定义
Grep(
  pattern="enum ",
  glob="**/*.php",
  output_mode="content",
  -A=15
)

# 4. 搜索事件/消息类
Grep(
  pattern="class.*Event|class.*Message",
  glob="**/*.php",
  output_mode="content",
  -A=30
)
```

**检索信息**:
- **数据模型名称**: 模型类名
- **字段定义**: 字段名、类型、约束、说明
- **关系定义**: 外键、关联关系
- **索引和约束**: 唯一约束、索引定义
- **代码位置**: 文件路径和行号

**输出表格**:

| 模型名称 | 用途 | 主要字段 | 来源 | 代码位置 | 增量/全新 |
|---------|------|---------|------|---------|----------|
| xxxModel | xx配置表 | id, name, config_data | 知识库 | models/xxx.py:20 | 增量 |
| xxxConfigMessage | xx配置变更消息 | config_id, action, timestamp | 项目代码 | schemas/xxx_config.py:15 | 增量 |

#### 4. 判断识别内容

**执行目标**: 判断是否提取到配置文件需求和全局数据结构定义需求，**若没有这类需求，结束执行，输出：不涉及**

**生成内容结构：**

```
5. 数据结构设计
- 5.1 配置文件定义
  不涉及

- 5.2 全局数据结构定义
  不涉及
```

#### 5. 汇总输出数据结构需求表格

**执行目标**: 将知识库检索、配置文件检索和全局数据结构检索的结果汇总,输出统一的位置表格文件。

**输出格式：**

输出数据接口需求表格，只需要按照表格格式输出，不需要输出其他内容，路径`doc/design/`，文件名`数据结构需求_check.md`（不允许删除）

| 数据结构  | 结构类型     | 功能类型 | 描述              | 代码位置         |
| --------- | ------------ | -------- | ----------------- | ---------------- |
| xxxConfig | 配置文件     | 全新     | xxx功能的配置文件 | xxx.py line:20   |
| xxx_model | 全局数据结构 | 增量     | xxx传输类型       | xxx.java line:20 |
| xxx_table | 表结构       | 增量     | xxx表             | xxx.go line:20   |

**输出文件**: `doc/design/数据结构需求_check.md`

**表格内容**:

- 包含步骤2.0知识库数据结构检索的表格(优先级最高)
- 包含步骤2.1配置文件检索的表格(补充和验证)
- 包含步骤2.2全局数据结构检索的表格(补充和验证)
- 清晰标注每个数据结构的来源(知识库/项目代码)
- 清晰标注每个数据结构的代码位置、用途、是否为增量开发

**数据结构来源优先级**:
1. **知识库**: 优先使用知识库中的数据结构定义(已归档、已验证)
2. **项目代码**: 补充知识库中未覆盖的数据结构,或验证知识库信息的准确性

**注意**: 执行完成后不删除此检查点文件,步骤3和步骤4需要读取此文件。

#### 6. 完成检查点（强制执行）

**⚠️ 关键操作：必须将数据结构需求表格写入检查点文件**

**强制执行步骤**：
1. **使用Write工具**将步骤2生成的完整数据结构需求表格写入检查点文件 `doc/design/数据结构需求_check.md`
2. **使用Read工具**读取检查点文件，验证表格已正确写入
3. 如果验证失败，重新执行写入操作

**验证清单**：

- [ ] 检查点文件 `doc/design/数据结构需求_check.md` 已创建
- [ ] 知识库检索结果表格已写入（如有）
- [ ] 配置文件检索结果表格已写入（如有）
- [ ] 全局数据结构检索结果表格已写入（如有）
- [ ] 每个数据结构都标注了来源（知识库/项目代码）
- [ ] 每个数据结构都有代码位置信息
- [ ] 表格格式符合Markdown规范

**如果验证失败**：
- 检查检查点文件路径是否正确
- 检查表格格式是否符合Markdown规范
- 确认所有检索结果都已包含在表格中
- 重新执行写入操作直到验证通过

------

### 步骤3: 生成5.1 配置文件定义

**前置条件**: 读取步骤2中生成的`doc/design/数据结构需求_check.md`

**执行目标**: 生成完整的配置文件定义,包括配置项说明、存储方式、验证规则。

**增量配置设计原则**:
- 基于现有配置文件格式进行扩展
- 保持配置项命名风格一致
- 新增配置项使用可选字段并提供默认值
- 标注新增配置项

**全新配置设计原则**:

- 遵循现有项目的配置文件风格
- 使用合理的配置分组
- 提供清晰的配置说明和示例
- 定义配置验证规则

#### 1. 检索代码仓库中的配置文件

根据数据结构需求_check.md表格中的配置文件信息,在代码仓库中检索完整配置定义:

```python
# 按配置文件路径逐个读取
for config_file in config_files:
    Read(file_path=config_file)
    # 提取配置项定义、类型、默认值、说明
```

#### 2. 生成配置文件定义

**生成内容结构**:

```
##### 5.1.1 配置文件格式

描述配置文件的存储格式和加载方式:

**配置存储方式**:

- 配置文件格式: Python类 (Pydantic BaseSettings)
- 配置文件路径: `xxx/configs/settings.xx`
- 环境变量支持: 支持通过环境变量覆盖配置项
- 配置加载时机: 应用启动时加载

**配置分组**:

- 数据库配置: DatabaseConfig
- Redis配置: RedisConfig
- 业务配置: BusinessConfig
- 日志配置: LogConfig

##### 5.1.2 配置项定义

对于每个配置分组,详细定义配置项:

**配置分组1: 数据库配置 (DatabaseConfig)**
| 配置项   | 类型 | 默认值      | 必填 | 说明        | 验证规则         | 增量/全新 |
| -------- | ---- | ----------- | ---- | ----------- | ---------------- | --------- |
| xxx_host | str  | "localhost" | 是   | xxx主机地址 | 合法的主机名或IP | 已有      |

##### 5.1.3 配置升级流程

如果需要在原有配置基础上升级,描述配置的升级流程:

- **配置版本**
- **升级内容**
- **升级兼容性**
- **升级步骤**
```

#### 3. 完成检查点（强制执行）

**⚠️ 关键操作：必须将5.1章节内容写入目标文件**

**强制执行步骤**：

1. **使用Edit工具**将步骤3生成的完整5.1 配置文件定义章节追加到目标文件
2. **使用Read工具**读取目标文件，验证5.1章节已正确写入
3. 如果验证失败，重新执行写入操作

**验证清单**：

- [ ] 5.1章节标题"### 5.1. 配置文件定义"存在于目标文件
- [ ] 5.1.1配置文件格式子章节完整（包含配置存储方式和配置分组）
- [ ] 5.1.2配置项定义子章节完整（包含所有配置分组的配置项表格）
- [ ] 5.1.3配置升级流程子章节完整（如涉及升级）或标注"不涉及"
- [ ] 所有配置项表格包含"增量/全新"列
- [ ] 章节格式符合Markdown规范

**如果验证失败**：
- 检查目标文件路径是否正确
- 检查Edit工具的old_string定位是否准确
- 确认所有配置项都已包含在表格中
- 验证表格格式是否符合Markdown规范
- 重新执行写入操作直到验证通过

------

### 步骤4: 生成5.2 全局数据结构定义

**前置条件**: 读取步骤2中生成的`doc/design/数据结构需求_check.md`

**执行目标**: 生成完整的全局数据结构定义,包括ORM模型、Pydantic模型、枚举类型、消息结构。

**增量数据结构设计原则**:
- 基于现有数据模型进行扩展
- 新增字段使用可选字段避免破坏性变更
- 保持字段命名风格一致
- 考虑数据库迁移兼容性

**全新数据结构设计原则**:
- 遵循现有项目的数据模型风格
- 使用清晰的字段命名
- 提供完整的字段说明
- 定义必要的索引和约束

#### 1. 检索代码仓库中的数据结构

根据数据结构需求_check.md表格中的信息,在代码仓库中检索完整数据结构定义:

```python
# 按数据模型文件逐个读取
for model_file in model_files:
    Read(file_path=model_file)
    # 提取模型定义、字段、关系、约束
```

#### 2. 生成全局数据结构定义

**生成内容结构**:

```markdown
##### 5.2.1 枚举类型定义

对于枚举类型,详细定义枚举值:

**枚举类型1: xxx操作类型 (xxxActionType)**

**枚举用途**: 定义xxx配置变更的操作类型

**代码位置**: `xxx/models/xxx.py:90-95`

**枚举值定义**:
| 枚举值 | 数值     | 说明   | 增量/全新 |
| ------ | -------- | ------ | --------- |
| CREATE | "create" | 创建xx | 已有      |
| UPDATE | "update" | 更新xx | 已有      |
| DELETE | "delete" | 删除xx | 已有      |
| MERGE  | "merge"  | 合并xx | 新增      |

**完整枚举定义**:
class xxxActionType(str, Enum):
    """xxx操作类型"""
    CREATE = "create"
    UPDATE = "update"
    DELETE = "delete"
    MERGE = "merge"  # 新增

##### 5.2.4 消息结构定义

对于消息队列中的消息,详细定义消息格式:

**消息结构1: xxx配置变更消息 (xxConfigChangeMessage)**

**消息用途**: 通知其他模块xx区域配置发生变更

**Topic名称**: `persistent://tenant/namespace/xx-config-change`

**代码位置**: `xxxx/xxxx/xx_config.py:15-30`

**字段定义**:
| 字段名  | 数据类型 | 必填 | 说明                                 | 增量/全新 |
| ------- | -------- | ---- | ------------------------------------ | --------- |
| xxx_id  | int      | 是   | 区域ID                               | 已有      |
| action  | str      | 是   | 操作类型(create/update/delete/merge) | 已有      |
| xx_name | str      | 是   | xx名称                               | 全新      |

**完整消息定义**:
class xxConfigChangeMessage(BaseModel):
    """xx配置变更消息"""
    xx_id: int = Field(..., description="xxID")
    action: IPRegionActionType = Field(..., description="操作类型")
    xx_name: str = Field(..., description="xx名称")

**完整消息示例**:
{
  "region_id": 1,
  "action": "merge",
  "region_name": "合并后xx"
}

##### 5.2.5 全局常量定义

对于全局常量,详细定义常量值:

**常量分组1: xxx配置常量**

**代码位置**: `xxx/utils/constants.py:50-60`
| 常量名                  | 类型 | 值   | 说明                 | 增量/全新 |
| ----------------------- | ---- | ---- | -------------------- | --------- |
| DEFAULT_REGION_PRIORITY | int  | 1    | 默认区域优先级       | 已有      |
| MAX_MERGE_REGIONS       | int  | 10   | 单次合并最大区域数量 | 新增      |


**完整常量定义**:
xxxxxxxxxx # xxx配置常量
```

#### 3. 完成检查点（强制执行）

**⚠️ 关键操作：必须将5.2章节内容写入目标文件**

**强制执行步骤**：
1. **使用Edit工具**将步骤4生成的完整5.2 全局数据结构定义章节追加到目标文件
2. **使用Read工具**读取目标文件，验证5.2章节已正确写入
3. 如果验证失败，重新执行写入操作

**验证清单**：
- [ ] 5.2章节标题"### 5.2. 全局数据结构定义"存在于目标文件
- [ ] 5.2.1枚举类型定义子章节完整（如涉及枚举）或标注"不涉及"
- [ ] 5.2.4消息结构定义子章节完整（如涉及消息）或标注"不涉及"
- [ ] 5.2.5全局常量定义子章节完整（如涉及常量）或标注"不涉及"
- [ ] 所有数据结构表格包含"增量/全新"列
- [ ] 所有数据结构都有完整的代码示例
- [ ] 章节格式符合Markdown规范

**如果验证失败**：
- 检查目标文件路径是否正确
- 检查Edit工具的old_string定位是否准确
- 确认所有数据结构都已包含在章节中
- 验证代码格式和表格格式是否正确
- 重新执行写入操作直到验证通过

------

### 步骤5: 生成5.3 数据表结构定义

**前置条件**: 读取步骤2中生成的`doc/design/数据结构需求_check.md`

**执行目标**: 生成完整的数据表结构定义，包括表字段定义、表约束规则、表关联配置、表分区/分表规则。

**增量数据表设计原则**:

- 基于现有表结构进行扩展
- 新增字段保持与现有表命名风格一致
- 考虑索引策略的兼容性
- 保持外键约束的一致性

**全新数据表设计原则**:

- 遵循现有项目的数据库设计规范
- 使用清晰的字段命名和注释
- 提供完整的约束规则说明
- 定义合理的默认值和索引策略

#### 1. 检索代码仓库中的表结构

根据数据结构需求_check.md表格中的信息，在代码仓库中检索现有数据表定义:

```python
# 按数据表定义文件逐个读取
for table_file in table_files:
    Read(file_path=table_file)
    # 提取表字段、约束规则、索引定义
```

#### 2. 生成数据表结构定义

**生成内容结构**:

```markdown
##### 5.3.1 数据表字段定义

对于数据表字段，详细定义字段属性:

**表1: xxx配置表 (t_xxx_config)**

**表用途**: 存储xxx配置信息

**代码位置**: `xxx/models/xxx_config.sql:10-50`

**字段定义**:
| 字段名 | 数据类型 | 约束 | 默认值 | 索引 | 说明 | 增量/全新 |
| ------ | -------- | ---- | ------ | ---- | ---- | --------- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | - | PRIMARY | 主键ID | 已有 |
| name | VARCHAR(100) | NOT NULL | - | INDEX | 配置名称 | 已有 |
| type | VARCHAR(50) | NOT NULL | 'default' | INDEX | 配置类型 | 已有 |
| priority | INT | - | 1 | - | 优先级 | 新增 |
| description | TEXT | - | NULL | - | 描述信息 | 新增 |
| created_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | - | 创建时间 | 已有 |
| updated_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE | - | 更新时间 | 已有 |

##### 5.3.2 表约束规则

对于表约束，详细定义约束规则:

**表1: xxx配置表约束规则**

**代码位置**: `xxx/models/xxx_config.sql:55-80`

| 约束名称 | 约束类型 | 涉及字段 | 约束说明 | 增量/全新 |
| -------- | -------- | -------- | -------- | --------- |
| pk_xxx_config | PRIMARY KEY | id | 主键约束 | 已有 |
| uk_xxx_name | UNIQUE | name, type | 名称+类型唯一 | 已有 |
| fk_xxx_category | FOREIGN KEY | category_id | 关联分类表 | 新增 |
| ck_xxx_priority | CHECK | priority | priority >= 1 AND priority <= 100 | 新增 |

**完整约束定义**:
-- 主键约束
ALTER TABLE t_xxx_config ADD CONSTRAINT pk_xxx_config PRIMARY KEY (id);

-- 唯一约束
ALTER TABLE t_xxx_config ADD CONSTRAINT uk_xxx_name UNIQUE (name, type);

-- 外键约束
ALTER TABLE t_xxx_config ADD CONSTRAINT fk_xxx_category
    FOREIGN KEY (category_id) REFERENCES t_category(id) ON DELETE CASCADE;

-- 检查约束
ALTER TABLE t_xxx_config ADD CONSTRAINT ck_xxx_priority
    CHECK (priority >= 1 AND priority <= 100);

##### 5.3.3 表关联配置

对于表关联，详细定义表间关系:

**关联关系1: xxx配置表 - 分类表**

**关联类型**: 多对一 (N:1)

**代码位置**: `xxx/models/xxx_config.sql:85-100`

| 主表 | 关联表 | 关联字段 | 关联类型 | 级联策略 | 增量/全新 |
| ---- | ------ | -------- | -------- | -------- | --------- |
| t_xxx_config | t_category | category_id -> id | N:1 | ON DELETE CASCADE | 新增 |
| t_xxx_config | t_user | created_by -> id | N:1 | ON DELETE SET NULL | 已有 |
| t_xxx_config | t_xxx_detail | id <- config_id | 1:N | ON DELETE CASCADE | 新增 |

**级联策略说明**:
- CASCADE: 主表删除时，关联记录同步删除
- SET NULL: 主表删除时，关联字段置为NULL
- RESTRICT: 存在关联记录时，禁止删除主表记录
- NO ACTION: 延迟检查，事务结束时验证

##### 5.3.4 表分区/分表规则

对于表分区和分表，详细定义分区策略:

**分区规则1: xxx配置表按时间分区**

**分区类型**: RANGE分区 / HASH分区 / LIST分区

**代码位置**: `xxx/models/xxx_config.sql:105-120`

| 表名 | 分区类型 | 分区字段 | 分区规则 | 分区数量 | 增量/全新 |
| ---- | -------- | -------- | -------- | -------- | --------- |
| t_xxx_log | RANGE | created_at | 按月分区 | 12 | 新增 |
| t_xxx_data | HASH | user_id | 按用户ID哈希 | 8 | 新增 |

**分区定义示例**:
-- 按时间范围分区
CREATE TABLE t_xxx_log (
    id BIGINT PRIMARY KEY,
    created_at DATETIME NOT NULL,
    ...
) PARTITION BY RANGE (YEAR(created_at) * 100 + MONTH(created_at)) (
    PARTITION p202401 VALUES LESS THAN (202402),
    PARTITION p202402 VALUES LESS THAN (202403),
    ...
);

**分表规则**（如涉及）:
| 表名 | 分表策略 | 分表字段 | 分表数量 | 路由规则 | 增量/全新 |
| ---- | -------- | -------- | -------- | -------- | --------- |
| t_xxx_order | 水平分表 | user_id | 16 | user_id % 16 | 新增 |

**增量/全新**: 新增
```

#### 3. 完成检查点（强制执行）

**⚠️ 关键操作：必须将5.3章节内容写入目标文件**

**强制执行步骤**：

1. **使用Edit工具**将步骤5生成的完整5.3 数据表结构定义章节追加到目标文件
2. **使用Read工具**读取目标文件，验证5.3章节已正确写入
3. 如果验证失败，重新执行写入操作

**验证清单**：

- [ ] 5.3章节标题"### 5.3. 数据表结构定义"存在于目标文件
- [ ] 5.3.1数据表字段定义子章节完整（如涉及数据表）或标注"不涉及"
- [ ] 5.3.2表约束规则子章节完整（如涉及约束）或标注"不涉及"
- [ ] 5.3.3表关联配置子章节完整（如涉及关联）或标注"不涉及"
- [ ] 5.3.4表分区/分表规则子章节完整（如涉及分区分表）或标注"不涉及"
- [ ] 所有数据表结构表格包含"增量/全新"列
- [ ] 所有数据表结构都有完整的SQL示例
- [ ] 章节格式符合Markdown规范

**如果验证失败**：

- 检查目标文件路径是否正确
- 检查Edit工具的old_string定位是否准确
- 确认所有数据表结构都已包含在章节中
- 验证SQL格式和表格格式是否正确
- 重新执行写入操作直到验证通过

------

### 步骤6: 章节完整性检查与文件验证

**执行目标**: 验证第5章所有章节已正确写入目标文件。

#### 1. 文件验证

**使用Read工具**读取目标文件，检查以下章节是否存在且完整：

| 章节 | 必须包含内容 |
|------|-------------|
| 5.1 配置文件定义 | 配置文件格式、配置项定义、配置升级流程（或"不涉及"） |
| 5.2 全局数据结构定义 | 枚举类型、消息结构、全局常量（或"不涉及"） |
| 5.3 数据表结构定义 | 表字段、约束规则、表关联、分区分表（或"不涉及"） |

#### 2. 通用检查项

- [ ] 所有表格包含"增量/全新"列
- [ ] 所有数据结构有代码位置信息
- [ ] 表格和代码格式符合Markdown规范
- [ ] 需求文档中所有数据结构都已设计
- [ ] 新增字段使用可选字段或提供默认值

#### 3. 验证失败处理

- **章节缺失**: 返回对应步骤（3/4/5）重新执行写入
- **内容不完整**: 补充缺失内容后重新写入
- **格式不规范**: 修正格式后重新写入

**步骤完成标记**: 所有章节验证通过后，第5章内容写入完成。

---

## 注意事项

### 多语言支持要点

**检索策略选择**:
- 根据项目主要编程语言选择对应的检索模式
- 如果是多语言混合项目,需要分别使用多种检索模式
- 优先检索主要代码目录,避免检索第三方库和依赖包

**不同语言的数据结构特点**:

| 语言 | ORM框架 | 数据模型 | 枚举类型 | 配置文件 |
|------|---------|---------|---------|---------|
| Python | SQLAlchemy, Django ORM | Pydantic, Dataclass | Enum, IntEnum | .py, .yaml, .toml, .ini |
| Java | JPA/Hibernate, MyBatis | DTO, POJO | enum | .properties, .xml, .yaml |
| Go | GORM, sqlx | struct | const+iota | .yaml, .toml, .json |
| PHP | Doctrine, Eloquent | Model, DTO | enum (8.1+) | .php, .ini, .yaml |

**检索注意事项**:
- **Python**: 注意区分SQLAlchemy的Base继承和Django的Model继承
- **Java**: 注意JPA注解(@Entity, @Table)和MyBatis的XML映射
- **Go**: 注意struct的tag定义(json, gorm, validate等)
- **PHP**: 注意Doctrine的注解和Eloquent的属性定义

**输出格式统一**:
- 无论使用何种语言,输出的设计文档格式保持一致
- 代码示例使用项目实际使用的语言
- 字段类型使用各语言的原生类型名称
- 字段类型使用各语言的原生类型名称

### 增量开发要点
- **知识库优先**: 优先使用 `{所属业务仓库}/doc/kb/技术知识库/数据结构.md` 进行快速检索,获取已归档的数据结构定义
- **项目代码辅助**: 配合项目代码检索进行补充和验证,确保信息完整性
- **谨慎修改**: 修改现有数据结构时,必须评估向后兼容性
- **明确标注**: 清晰标注数据结构是"新增"、"修改"还是"复用",以及来源(知识库/项目代码)
- **兼容性优先**: 修改数据结构时优先考虑向后兼容,避免破坏性变更
- **双向验证**: 知识库信息需要与项目代码实际情况进行核对验证

### 数据结构设计原则
- **类型准确**: 字段类型定义要准确,避免类型转换错误
- **约束完整**: 必要的约束条件要完整定义(非空、唯一、范围等)
- **索引合理**: 基于查询场景设计合理的索引
- **性能考虑**: 考虑数据量和查询性能,避免过度设计

### 数据库设计规范
- **命名规范**: 表名、字段名使用统一的命名风格
- **主键设计**: 每个表必须有主键,优先使用自增整型
- **外键约束**: 明确定义外键关系,保证数据一致性
- **索引优化**: 基于查询场景建立索引,避免过多索引
- **字段长度**: 字符串类型明确定义最大长度

### 配置设计规范
- **分组合理**: 按功能模块对配置项进行分组
- **默认值**: 非必填配置项必须提供合理的默认值
- **环境变量**: 支持通过环境变量覆盖配置
- **验证规则**: 定义配置项的验证规则,启动时验证

### 文档完整性
- **字段说明**: 每个字段必须有清晰的说明
- **示例提供**: 提供完整的数据结构示例
- **约束明确**: 明确字段的约束条件
- **迁移方案**: 如有表结构变更,提供迁移脚本

### 向后兼容性
- **新增字段**: 使用可选字段或提供默认值
- **字段删除**: 不删除现有字段,使用软删除或标记废弃
- **类型变更**: 避免直接修改字段类型,考虑新增字段过渡
- **数据迁移**: 提供数据迁移脚本和回滚方案

---

## 总结

本命令专注于生成高质量的数据结构设计章节,确保:
1. **完整性**: 覆盖配置文件、全局数据结构和表结构的所有需求
2. **一致性**: 保持与现有代码风格的一致性
3. **兼容性**: 增量开发时考虑向后兼容性
4. **规范性**: 遵循数据库设计和配置管理的最佳实践
5. **可维护性**: 提供完整的文档和示例,便于后续维护
6. **多语言支持**: 支持Python、Java、Go、PHP等主流编程语言的数据结构检索和设计
7. **高效检索**: 优先使用知识库快速检索,减少Token消耗

**核心能力**:
- ✅ **知识库优先**: 优先使用技术知识库进行快速检索,显著提升效率
- ✅ **智能语言识别**: 自动识别项目使用的编程语言
- ✅ **多框架支持**: 支持各语言主流ORM框架和数据模型框架
- ✅ **增量开发友好**: 优先检索复用现有代码,避免重复造轮子
- ✅ **统一输出格式**: 无论何种语言,输出的设计文档格式统一规范
- ✅ **向后兼容保障**: 严格检查数据结构变更的兼容性影响
- ✅ **双向验证**: 知识库信息与项目代码双重验证,确保准确性

