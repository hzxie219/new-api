# {功能名称} - 详细设计文档

> 本文档使用 RFC 2119 规范语言：必须（SHALL）、应该（SHOULD）、可以（MAY）

---

## 1. 概述

### 1.1 为什么要做

[1-2 句话说明为什么要做这个功能，解决什么问题或带来什么机会]

### 1.2 变更内容

- [变更项1]
- [变更项2]
- [如有破坏性变更，标记为 **破坏性变更**: 变更说明]

### 1.3 影响范围

- **受影响的功能模块**: [功能模块列表]
- **受影响的代码文件**: [关键文件/系统]

---

## 2. 目标与非目标

### 2.1 目标

1. [目标1]
2. [目标2]
3. [目标3]

### 2.2 非目标

明确不在本次范围内的内容：

- [非目标1]
- [非目标2]

---

## 3. 技术决策

> **重要**：每个目标都必须有对应的技术决策，不能遗漏。
> 如果某个模块可以设计为通用模块供其他功能复用，必须在决策中说明。
>
> **横切关注点设计原则**：
> - 对于**监控埋点、日志记录、认证鉴权**等横切关注点，优先采用**中间件**或**通用可复用模块**实现，而非在每个接口中单独实现
> - 新增的基础能力（如文件写入、缓存管理等）应设计为**通用模块**，供其他功能复用

### 3.1 决策1: [对应目标1的决策标题]

**决策内容**: [具体决策内容及原因]

**备选方案**:

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| 方案A | ... | ... | 采纳/放弃 |
| 方案B | ... | ... | 采纳/放弃 |

**选择理由**: [为什么选择该方案]

---

### 3.2 决策2: [对应目标2的决策标题]

**决策内容**: [具体决策内容及原因]

**备选方案**:

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| 方案A | ... | ... | 采纳/放弃 |
| 方案B | ... | ... | 采纳/放弃 |

**选择理由**: [为什么选择该方案]

---

<!-- 继续为每个目标添加对应的决策... -->

---

<!-- 继续为每个目标添加对应的决策... -->

## 4. 整体架构

[使用文本图或描述说明整体架构]

## 5. 模块设计

### 5.1 目录结构

```
[列出新增/修改的目录结构]
```

### 5.2 核心流程

[描述核心业务流程]

### 5.3 接口定义

#### 模块

具体模块的数据结构接口定义

## 6. 需求规格

### 6.1 需求1: [功能名称]

系统**必须**提供 [功能描述]...

#### 接口定义（如适用）

- **路径**: `[HTTP方法] [接口路径]`
- **Content-Type**: `application/json`

#### 请求参数

> **重要**：必须明确每个参数的必填/非必填状态

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| [参数1] | string | 是 | [说明] |
| [参数2] | int | 否 | [说明] |

#### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| [参数1] | string | [说明] |
| [参数2] | object | [说明] |

#### 业务逻辑说明（如适用）

> **重要**：业务逻辑判断必须明确说明数据来源和计算规则

[字段名] 的判断**必须**基于以下规则：
- 数据来源：[说明数据从哪里获取]
- 计算规则：[说明具体的计算/比较逻辑]

#### 场景: [成功场景名称]

- **前置条件**: [前置条件]
- **当**: [用户操作/触发条件]
- **则**: [期望结果]

#### 场景: [异常场景名称]

- **前置条件**: [前置条件]
- **当**: [异常触发条件]
- **则**: [异常处理结果]

---

### 6.2 需求2: [调用外部接口]（如适用）

系统**必须**调用 [外部接口名称] 获取 [数据说明]。

#### 接口调用

- **目标接口**: `[HTTP方法] [接口路径]`
- **认证方式**: [认证说明]

#### 请求参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| [参数1] | string | [说明] |

#### 响应数据结构

> **重要**：调用外部接口时，必须写明完整的响应数据结构

| 参数名 | 类型 | 说明 |
|--------|------|------|
| code | string | 响应码 |
| message | string | 响应消息 |
| data | object | 响应数据 |
| data.[字段1] | [类型] | [说明] |
| data.[字段2] | [类型] | [说明] |

---

### 6.3 非功能性需求

#### 性能要求

- [性能要求1]
- [性能要求2]

#### 可靠性要求

- [可靠性要求]

---

## 7. 必须复用的现有资源

> **此章节的目的**：确保编码阶段能够复用现有资源，避免重复造轮子。

### 7.1 常量

| 常量名 | 定义位置 | 用途 |
|--------|---------|------|
| SuccessCode | xxx/public_struct.go:10 | 成功状态码 |

### 7.2 工具函数

| 函数名 | 定义位置 | 用途 |
|--------|---------|------|
| ParseResponseStatus | xxx/metrics_http_client.go:50 | 解析响应状态 |

### 7.3 数据结构

| 结构体名 | 定义位置 | 用途 |
|----------|---------|------|
| GpuChatParams | xxx/gpt_client.go:20 | LLM 调用参数 |
